language: node_js

notifications:
  email: false

node_js:
  - '7'
  - '6'

services:
  - postgresql

addons:
  postgresql: '9.4'

before_script:
  - psql -c 'create database mock;' -U postgres
  - npm prune

after_success:
  - npm run semantic-release

branches:
  except:
    - /^v\d+\.\d+\.\d+$/